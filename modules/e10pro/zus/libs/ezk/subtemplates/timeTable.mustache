<div class="container-fluid" style="overflow-y: auto; height: 100%;">
  <h1 class="p-1 pt-3">{{{@icon;icon:user/clock}}} Rozvrh hodin</h1>

  {{^data.timeTable.days}}
    Počkejte, prosím, na vytvoření rozvrhu.
  {{/data.timeTable.days}}

  {{#data.timeTable.days}}
    <ol class="shpd-list mb-2">
      <li class="shpd-list-item align-items-start">
        <h3 class="fw-bold">{{dayName}}</h3>

        {{#hours}}
          <div class="mt-2 me-auto">
            <h5 class="mb-0"><span class="fw-bold">{{timeBegin}} - {{timeEnd}}</span><span class="fw-normal"> - {{subjectTitle}}</span></h5>
            {{teacher}}, {{place}}
          </div>
        {{/hours}}
      </li>
    </ol>
  {{/data.timeTable.days}}
</div>